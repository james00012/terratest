# Terraform files
.terraform
terraform.tfstate
terraform.tfvars
terraform.tfvars.json
*.tfstate*
.terragrunt
.terragrunt-cache
.terraform.lock.hcl
# IDE files
.idea
.vscode
*.iml
vendor

# Folder used to store temporary test data by Terratest
.test-data

# rbenv
.ruby-version

# OS X
.DS_Store
# Intermediate file for testing
kubeconfig

# environment files
.env

# v2: compiled cmd binaries (built locally during dev or CI test)

# v2 modularization: compiled cmd binaries (built locally or by CI builds)
cmd/pick-instance-type/pick-instance-type
cmd/terratest_log_parser/terratest_log_parser
